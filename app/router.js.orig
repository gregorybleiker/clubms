import Ember from 'ember';

var Router = Ember.Router.extend({
  location: CcmsENV.locationType
});

Router.map(function () {
  this.resource('pages', {path: '/'},
		function() {
		  this.route('page', { path: '/page/:page_id'});
		});
});

export default Router;
